APP_URL=https://your-vercel-url.vercel.app
SESSION_SECRET=replace-with-a-long-random-secret
STRIPE_MODE=demo
STRIPE_SECRET_KEY=
STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
# Add this in Vercel Environment Variables with the Gmail address that will send payment notifications.
GMAIL_SMTP_EMAIL=
# Add this in Vercel Environment Variables with the Gmail App Password for the address above. Never hardcode it in code.
GMAIL_SMTP_APP_PASSWORD=
